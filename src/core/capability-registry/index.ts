/**
 * Capability Registry — frontier contract (Bloque 4 · ADR-014).
 * No runtime or project binding in this block.
 */
export interface CapabilityRegistry {
  readonly __frontier: "CapabilityRegistry";
}

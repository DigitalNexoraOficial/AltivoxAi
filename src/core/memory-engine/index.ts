/**
 * Memory Engine — frontier contract (Bloque 4 · ADR-014).
 * No runtime. Distinct from docs/MEMORY.md (human/repo narrative).
 */
export interface MemoryEngine {
  readonly __frontier: "MemoryEngine";
}
